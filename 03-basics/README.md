# Options
My first application built on Node.js :D

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Base of the multiply talbe                 [número] [requerido]
  -l, --list     List the table in the terminal      [booleano] [defecto: false]
  -h, --hasta    Numero limitante por el cual se va a multiplicar       [número]
```