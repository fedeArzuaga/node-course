
// ES5 and older
/* function sumar (a, b) {
    return a + b;
} */

// ESNext!
const sumar = ( a, b ) => a + b;

const saludar = () => 'HelloWorld!';

console.log( sumar(5, 10) );
console.log( saludar() );